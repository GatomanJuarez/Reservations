function aa() {
    console.log("HH");
    var contenedor = new Array();

    leeArchivo1("txt/1/reservados1.txt");
    leeArchivo2("txt/1/reservados2.txt");
    leeArchivo3("txt/1/reservados3.txt");
    leeArchivo4("txt/1/reservados4.txt");
    leeArchivo5("txt/1/reservados5.txt");
    leeArchivo6("txt/1/reservados6.txt");
    leeArchivo7("txt/1/reservados7.txt");
    leeArchivo8("txt/1/reservados8.txt");
    leeArchivo9("txt/1/reservados9.txt");
    leeArchivo10("txt/1/reservados10.txt");
    leeArchivo11("txt/1/reservados11.txt");
    leeArchivo12("txt/1/reservados12.txt");

    leeArchivo13("txt/2/reservados13.txt");
    leeArchivo14("txt/2/reservados14.txt");
    leeArchivo15("txt/2/reservados15.txt");
    leeArchivo16("txt/2/reservados16.txt");
    leeArchivo17("txt/2/reservados17.txt");
    leeArchivo18("txt/2/reservados18.txt");
    leeArchivo19("txt/2/reservados19.txt");
    leeArchivo20("txt/2/reservados20.txt");
    leeArchivo21("txt/2/reservados21.txt");
    leeArchivo22("txt/2/reservados22.txt");
    leeArchivo23("txt/2/reservados23.txt");
    leeArchivo24("txt/2/reservados24.txt");

    leeArchivo25("txt/3/reservados25.txt");
    leeArchivo26("txt/3/reservados26.txt");
    leeArchivo27("txt/3/reservados27.txt");
    leeArchivo28("txt/3/reservados28.txt");
    leeArchivo29("txt/3/reservados29.txt");
    leeArchivo30("txt/3/reservados30.txt");
    leeArchivo31("txt/3/reservados31.txt");
    leeArchivo32("txt/3/reservados32.txt");
    leeArchivo33("txt/3/reservados33.txt");
    leeArchivo34("txt/3/reservados34.txt");
    leeArchivo35("txt/3/reservados35.txt");
    leeArchivo36("txt/3/reservados36.txt");

    leeArchivo37("txt/4/reservados37.txt");
    leeArchivo38("txt/4/reservados38.txt");
    leeArchivo39("txt/4/reservados39.txt");
    leeArchivo40("txt/4/reservados40.txt");
    leeArchivo41("txt/4/reservados41.txt");
    leeArchivo42("txt/4/reservados42.txt");
    leeArchivo43("txt/4/reservados43.txt");
    leeArchivo44("txt/4/reservados44.txt");
    leeArchivo45("txt/4/reservados45.txt");
    leeArchivo46("txt/4/reservados46.txt");
    leeArchivo47("txt/4/reservados47.txt");
    leeArchivo48("txt/4/reservados48.txt");

    leeArchivo49("txt/5/reservados49.txt");
    leeArchivo50("txt/5/reservados50.txt");
    leeArchivo51("txt/5/reservados51.txt");
    leeArchivo52("txt/5/reservados52.txt");
    leeArchivo53("txt/5/reservados53.txt");
    leeArchivo54("txt/5/reservados54.txt");
    leeArchivo55("txt/5/reservados55.txt");
    leeArchivo56("txt/5/reservados56.txt");
    leeArchivo57("txt/5/reservados57.txt");
    leeArchivo58("txt/5/reservados58.txt");
    leeArchivo59("txt/5/reservados59.txt");
    leeArchivo60("txt/5/reservados60.txt");

    leeArchivo61("txt/6/reservados61.txt");
    leeArchivo62("txt/6/reservados62.txt");
    leeArchivo63("txt/6/reservados63.txt");
    leeArchivo64("txt/6/reservados64.txt");
    leeArchivo65("txt/6/reservados65.txt");
    leeArchivo66("txt/6/reservados66.txt");
    leeArchivo67("txt/6/reservados67.txt");
    leeArchivo68("txt/6/reservados68.txt");
    leeArchivo69("txt/6/reservados69.txt");
    leeArchivo70("txt/6/reservados70.txt");
    leeArchivo71("txt/6/reservados71.txt");
    leeArchivo72("txt/6/reservados72.txt");

    leeArchivo73("txt/8/reservados73.txt");
    leeArchivo74("txt/8/reservados74.txt");
    leeArchivo75("txt/8/reservados75.txt");
    leeArchivo76("txt/8/reservados76.txt");
    leeArchivo77("txt/8/reservados77.txt");
    leeArchivo78("txt/8/reservados78.txt");
    leeArchivo79("txt/8/reservados79.txt");
    leeArchivo80("txt/8/reservados80.txt");
    leeArchivo81("txt/8/reservados81.txt");
    leeArchivo82("txt/8/reservados82.txt");
    leeArchivo83("txt/8/reservados83.txt");
    leeArchivo84("txt/8/reservados84.txt");

    leeArchivo85("txt/9/reservados85.txt");
    leeArchivo86("txt/9/reservados86.txt");
    leeArchivo87("txt/9/reservados87.txt");
    leeArchivo88("txt/9/reservados88.txt");
    leeArchivo89("txt/9/reservados89.txt");
    leeArchivo90("txt/9/reservados90.txt");
    leeArchivo91("txt/9/reservados91.txt");
    leeArchivo92("txt/9/reservados92.txt");
    leeArchivo93("txt/9/reservados93.txt");
    leeArchivo94("txt/9/reservados94.txt");
    leeArchivo95("txt/9/reservados95.txt");
    leeArchivo96("txt/9/reservados96.txt");

    leeArchivo97("txt/10/reservados97.txt");
    leeArchivo98("txt/10/reservados98.txt");
    leeArchivo99("txt/10/reservados99.txt");
    leeArchivo100("txt/10/reservados100.txt");
    //////////////////////////////////////////
    leeArchivo101("txt/11/reservados101.txt");
    leeArchivo102("txt/11/reservados102.txt");
    leeArchivo103("txt/11/reservados103.txt");
    leeArchivo104("txt/11/reservados104.txt");
    leeArchivo105("txt/11/reservados105.txt");
    leeArchivo106("txt/11/reservados106.txt");
    leeArchivo107("txt/11/reservados107.txt");
    leeArchivo108("txt/11/reservados108.txt");
    leeArchivo109("txt/11/reservados109.txt");
    leeArchivo110("txt/11/reservados110.txt");
    leeArchivo111("txt/11/reservados111.txt");
    leeArchivo112("txt/11/reservados112.txt");
    leeArchivo113("txt/11/reservados113.txt");

    leeArchivo114("txt/12/reservados114.txt");
    leeArchivo115("txt/12/reservados115.txt");
    leeArchivo116("txt/12/reservados116.txt");
    leeArchivo117("txt/12/reservados117.txt");
    leeArchivo118("txt/12/reservados118.txt");
    leeArchivo119("txt/12/reservados119.txt");
    leeArchivo120("txt/12/reservados120.txt");
    leeArchivo121("txt/12/reservados121.txt");
    leeArchivo122("txt/12/reservados122.txt");
    leeArchivo123("txt/12/reservados123.txt");
    leeArchivo124("txt/12/reservados124.txt");
    leeArchivo125("txt/12/reservados125.txt");

    leeArchivo126("txt/13/reservados126.txt");
    leeArchivo127("txt/13/reservados127.txt");
    leeArchivo128("txt/13/reservados128.txt");
    leeArchivo129("txt/13/reservados129.txt");
    leeArchivo130("txt/13/reservados130.txt");
    leeArchivo131("txt/13/reservados131.txt");
    leeArchivo132("txt/13/reservados132.txt");
    leeArchivo133("txt/13/reservados133.txt");
    leeArchivo134("txt/13/reservados134.txt");
    leeArchivo135("txt/13/reservados135.txt");
    leeArchivo136("txt/13/reservados136.txt");
    leeArchivo137("txt/13/reservados137.txt");

    leeArchivo138("txt/14/reservados138.txt");
    leeArchivo139("txt/14/reservados139.txt");
    leeArchivo140("txt/14/reservados140.txt");
    leeArchivo141("txt/14/reservados141.txt");
    leeArchivo142("txt/14/reservados142.txt");
    leeArchivo143("txt/14/reservados143.txt");
    leeArchivo144("txt/14/reservados144.txt");
    leeArchivo145("txt/14/reservados145.txt");
    leeArchivo146("txt/14/reservados146.txt");
    leeArchivo147("txt/14/reservados147.txt");
    leeArchivo148("txt/14/reservados148.txt");
    leeArchivo149("txt/14/reservados149.txt");

    leeArchivo150("txt/15/reservados150.txt");
    leeArchivo151("txt/15/reservados151.txt");
    leeArchivo152("txt/15/reservados152.txt");
    leeArchivo153("txt/15/reservados153.txt");
    leeArchivo154("txt/15/reservados154.txt");
    leeArchivo155("txt/15/reservados155.txt");
    leeArchivo156("txt/15/reservados156.txt");
    leeArchivo157("txt/15/reservados157.txt");
    leeArchivo158("txt/15/reservados158.txt");
    leeArchivo159("txt/15/reservados159.txt");
    leeArchivo160("txt/15/reservados160.txt");
    leeArchivo161("txt/15/reservados161.txt");

    leeArchivo162("txt/16/reservados162.txt");
    leeArchivo163("txt/16/reservados163.txt");
    leeArchivo164("txt/16/reservados164.txt");
    leeArchivo165("txt/16/reservados165.txt");
    leeArchivo166("txt/16/reservados166.txt");
    leeArchivo167("txt/16/reservados167.txt");
    leeArchivo168("txt/16/reservados168.txt");
    leeArchivo169("txt/16/reservados169.txt");
    leeArchivo170("txt/16/reservados170.txt");
    leeArchivo171("txt/16/reservados171.txt");
    leeArchivo172("txt/16/reservados172.txt");
    leeArchivo173("txt/16/reservados173.txt");

    leeArchivo174("txt/17/reservados174.txt");
    leeArchivo175("txt/17/reservados175.txt");
    leeArchivo176("txt/17/reservados176.txt");
    leeArchivo177("txt/17/reservados177.txt");
    leeArchivo178("txt/17/reservados178.txt");
    leeArchivo179("txt/17/reservados179.txt");
    leeArchivo180("txt/17/reservados180.txt");
    leeArchivo181("txt/17/reservados181.txt");
    leeArchivo182("txt/17/reservados182.txt");
    leeArchivo183("txt/17/reservados183.txt");
    leeArchivo184("txt/17/reservados184.txt");
    leeArchivo185("txt/17/reservados185.txt");

    leeArchivo186("txt/18/reservados186.txt");
    leeArchivo187("txt/18/reservados187.txt");
    leeArchivo188("txt/18/reservados188.txt");
    leeArchivo189("txt/18/reservados189.txt");
    leeArchivo190("txt/18/reservados190.txt");
    leeArchivo191("txt/18/reservados191.txt");
    leeArchivo192("txt/18/reservados192.txt");
    leeArchivo193("txt/18/reservados193.txt");
    leeArchivo194("txt/18/reservados194.txt");
    leeArchivo195("txt/18/reservados195.txt");
    leeArchivo196("txt/18/reservados196.txt");
    leeArchivo197("txt/18/reservados197.txt");

    leeArchivo198("txt/19/reservados198.txt");
    leeArchivo199("txt/19/reservados199.txt");
    leeArchivo200("txt/19/reservados200.txt");
}
//////////////////////////////////////////

function leeArchivo1(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[0] = Texto;
                //alert(contenedor[0]);
                console.log(contenedor[0] + "aaa");
            }
        }
    }
    archivo.send(null);
}

function leeArchivo2(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[1] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo3(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[2] = Texto;
                //console.log(contenedor[2]);
            }
        }
    }
    archivo.send(null);
}

function leeArchivo4(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[3] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo5(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[4] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo6(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[5] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo7(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[6] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo8(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[7] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo9(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[8] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo10(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[9] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo11(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[10] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo12(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[11] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo13(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[12] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo14(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[13] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo15(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[14] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo16(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[15] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo17(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[16] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo18(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[17] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo19(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[18] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo20(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[19] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo21(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[20] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo22(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[21] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo23(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[22] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo24(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[23] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo25(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[24] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo26(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[25] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo27(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[26] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo28(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[27] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo29(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[28] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo30(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[29] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo31(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[30] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo32(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[31] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo33(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[32] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo34(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[33] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo35(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[34] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo36(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[35] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo37(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[36] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo38(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[37] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo39(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[38] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo40(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[39] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo41(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[40] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo42(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[41] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo43(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[42] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo44(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[43] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo45(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[44] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo46(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[45] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo47(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[46] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo48(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[47] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo49(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[48] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo50(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[49] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo51(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[65] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo52(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[51] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo53(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[52] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo54(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[53] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo55(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[54] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo56(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[55] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo57(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[56] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo58(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[57] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo59(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[58] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo60(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[59] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo61(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[60] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo62(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[61] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo63(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[62] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo64(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[63] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo65(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[64] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo66(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[65] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo67(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[66] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo68(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[67] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo69(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[68] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo70(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[69] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo71(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[70] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo72(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[71] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo73(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[72] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo74(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[73] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo75(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[74] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo76(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[75] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo77(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[76] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo78(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[77] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo79(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[78] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo80(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[79] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo81(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[80] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo82(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[81] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo83(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[82] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo84(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[83] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo85(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[84] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo86(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[85] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo87(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[86] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo88(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[87] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo89(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[88] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo90(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[89] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo91(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[90] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo92(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[91] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo93(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[92] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo94(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[93] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo95(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[94] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo96(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[95] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo97(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[96] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo98(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[97] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo99(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[98] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo100(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[99] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////

function leeArchivo101(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[100] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo102(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[101] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo103(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[102] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo104(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[103] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo105(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[104] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo106(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[105] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo107(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[106] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo108(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[107] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo109(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[108] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo110(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[109] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo111(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[110] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo112(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[111] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo113(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[112] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo114(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[113] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo115(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[114] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo116(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[115] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo117(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[116] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo118(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[117] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo119(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[118] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo120(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[119] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo121(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[120] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo122(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[121] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo123(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[122] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo124(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[123] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo125(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[124] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo126(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[125] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo127(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[126] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo128(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[127] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo129(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[128] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo130(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[129] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo131(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[130] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo132(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[131] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo133(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[132] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo134(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[133] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo135(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[134] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo136(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[135] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo137(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[136] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo138(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[137] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo139(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[138] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo140(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[139] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo141(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[140] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo142(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[141] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo143(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[142] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo144(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[143] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo145(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[144] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo146(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[145] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo147(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[146] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo148(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[147] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo149(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[148] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo150(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[149] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo151(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[150] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo152(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[151] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo153(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[152] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo154(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[153] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo155(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[154] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo156(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[155] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo157(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[156] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo158(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[157] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo159(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[158] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo160(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[159] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo161(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[160] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo162(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[161] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo163(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[162] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo164(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[163] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo165(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[164] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo166(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[165] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo167(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[166] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo168(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[167] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo169(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[168] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo170(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[169] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo171(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[170] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo172(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[171] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo173(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[172] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo174(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[173] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo175(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[174] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo176(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[175] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo177(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[176] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo178(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[177] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo179(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[178] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo180(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[179] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo181(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[180] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo182(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[181] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo183(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[182] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo184(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[183] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo185(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[184] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo186(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[185] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo187(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[186] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo188(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[187] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo189(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[188] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo190(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[189] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
function leeArchivo191(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[190] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo192(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[191] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo193(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[192] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo194(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[193] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo195(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[194] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo196(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[195] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo197(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[196] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo198(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[197] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo199(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[198] = Texto;
            }
        }
    }
    archivo.send(null);
}

function leeArchivo200(file) {
    var archivo = new XMLHttpRequest();
    archivo.open("GET", file, false);
    archivo.onreadystatechange = function () {
        if (archivo.readyState === 4) {
            if (archivo.status === 200 || archivo.status == 0) {
                var Texto = archivo.responseText;
                //alert(Texto);
                reservadoSilla1 = Texto;
                contenedor[199] = Texto;
            }
        }
    }
    archivo.send(null);
}
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////


return contenedor;

//////////////

///
}


function sillasObtener() {
    console.log("HH");
    var contenedor;
    leeArchivo240("txt/sillaDisponibles.txt");

    function leeArchivo240(file) {
        var archivo = new XMLHttpRequest();
        archivo.open("GET", file, false);
        archivo.onreadystatechange = function () {
            if (archivo.readyState === 4) {
                if (archivo.status === 200 || archivo.status == 0) {
                    var Texto = archivo.responseText;
                    //alert(Texto);
                    reservadoSilla1 = Texto;
                    contenedor = Texto;
                }
            }
        }
        archivo.send(null);
    }
    return contenedor;
}

function datosObtener() {
    var silla1;
    leeArchivo500("txt/1/reservados1.txt");

    function leeArchivo500(file) {
        var archivo = new XMLHttpRequest();
        archivo.open("GET", file, false);
        archivo.onreadystatechange = function () {
            if (archivo.readyState === 4) {
                if (archivo.status === 200 || archivo.status == 0) {
                    var Texto = archivo.responseText;
                    //alert(Texto);
                    
                    silla1 = Texto;
                }
            }
        }
        archivo.send(null);
    }
    
    return silla1;
}
